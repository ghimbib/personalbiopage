---
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import SubscribeForm from '../components/SubscribeForm.astro';
import { webPageSchema } from '../utils/seo';

const title = 'Subscribe — Bibek Ghimire';
const description = 'Subscribe for occasional essays from Bibek Ghimire on product work, AI systems, practical building, and operating through messy technical spaces.';
---

<Layout
  title={title}
  description={description}
  canonicalPath="/subscribe/"
  jsonLd={webPageSchema({ path: '/subscribe/', title, description })}
>
  <Nav />
  <main id="main-content" class="pt-32 pb-20 px-6">
    <div class="max-w-3xl mx-auto">
      <a
        href="/blog"
        class="inline-flex items-center gap-2 text-text-muted hover:text-accent transition-colors text-sm font-mono mb-8"
        data-hover
      >
        <span>←</span>
        <span>Back to blog</span>
      </a>

      <header class="mb-12" data-animate>
        <p class="text-accent text-sm font-mono uppercase tracking-wider mb-3">Subscribe</p>
        <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold leading-tight mb-4">
          Get new essays in your inbox<span class="gradient-text">.</span>
        </h1>
        <p class="text-text-secondary text-lg leading-relaxed max-w-2xl">
          Occasional writing on product, systems, AI, and what I’m learning while building. No spam, no daily noise.
        </p>
      </header>

      <div data-animate data-delay="100">
        <SubscribeForm />
      </div>
    </div>
  </main>
  <Footer />
</Layout>
